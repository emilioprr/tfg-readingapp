spring.application.name=reading-app

# PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/readingapp}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Ribadeo1809}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=${PORT:8080}

# API Google Books
google.books.api.key=AIzaSyDqXKymmMt438Qf62DitjBHVchnLG-Xi8c

jwt.secret=${JWT_SECRET:rodriguezpulpeirojoseemilio20040918}
jwt.expiration=${JWT_EXPIRATION:86400000}